const DeliveryState = {
    Selected: 'Selected', // selected on UI
    Loading: 'Loading', // loading to server
    Submitted: 'Submitted', // submitted to server
    Failed: 'Failed' // loading failed
};

export default DeliveryState;
